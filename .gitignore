node_modules/
.env
data/*
!data/sources.json
output/
/uploads/
*.log
.DS_Store
.vscode/
.idea/

# Test coverage
coverage/

# Build artifacts
**/dist/
build/
.astro/

# Temporary files
*.tmp
*.temp

# Agent scratch space — unanchored on purpose: nothing in this repo is ever a
# source directory named .scratch, so catching it at any depth is safe. Contrast
# /uploads/ above, which must stay anchored or it also swallows core/src/uploads/.
.scratch/

# Design tooling sidecar
.impeccable/

# Local-only skills (voice guide)
.claude/skills/
