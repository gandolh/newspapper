---
/**
 * The proof sheet, at /kitchen-sink — DEV ONLY.
 *
 * It lives outside `src/pages/` on purpose: `astro.config.mjs` injects this
 * route when `command === 'dev'` and never at build time, so the page is a
 * working surface and not a public one. See that file for why.
 */
import App from '../layouts/App.astro';
import KitchenSinkIsland from '../components/KitchenSinkIsland';
---

<App title="Kitchen Sink">
  <KitchenSinkIsland client:load />
</App>
