import { logger } from '../utils/logger.js';

export async function queryEntitiesCommand(options: Record<string, unknown>): Promise<void> {
  logger.warn('Command not yet implemented');
  void options;
}
